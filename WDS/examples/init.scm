;(current-library-collection-paths (list (build-path (current-directory) "scheme")))
(current-library-collection-paths (list "c:/blake/integra/dynace.z/scheme"))

(require-library "Dynace.scm" "System")

