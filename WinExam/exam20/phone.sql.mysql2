

create table phone (
	fname	char(20) not null,
	lname	char(20) not null,
	street	char(30),
	age	smallint not null,
	id	integer,
	unique ( age, lname ),
	unique ( age, fname ),
	primary key (lname, fname)
);

{     anything between braces gets ignored

So the following table is ignored

create table vendors (
	id	integer primary key,
	name	char(30)
);


}