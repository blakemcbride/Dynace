

create table phone (
	fname	char(20),
	lname	char(20) not null,
	street	char(30),
	age	smallint,
	id	integer,
	primary key (lname, fname)
);

{
create table vendors (
	id	integer primary key,
	name	char(30)
);


}